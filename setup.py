from setuptools import find_packages, setup

setup(
    name='squareCount',
    packages=find_packages(include=['squareCount']),
    version='0.0.1',
    description='Counting squares of circles, triangles, etc.',
    url='',
    license='MIT',
    install_requires=[],
)

